import React from 'react'
import ReactDOM from 'react-dom/client'
import PokedexScanner from './PokedexScanner.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PokedexScanner />
  </React.StrictMode>,
)
